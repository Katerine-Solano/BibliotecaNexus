@model IEnumerable<BibliotecaNexus.Data.Domain.Entidades.Comentario>
@{
    ViewData["Title"] = "Comentarios";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Comentarios</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Crear Nuevo Comentario</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Cliente</th>
            <th>Libro</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.ComentarioId</td>
                <td>@item.Cliente.Nombre</td>
                <td>@item.Libro.Titulo</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.ComentarioId" class="btn btn-warning">Editar</a>
                    <a asp-action="Delete" asp-route-id="@item.ComentarioId" class="btn btn-danger">Eliminar</a>
                    <a asp-action="Details" asp-route-id="@item.ComentarioId" class="btn btn-info">Detalles</a>
                </td>
            </tr>
        }
    </tbody>
</table>
